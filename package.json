{
  "scripts": {
    "build": "esbuild src/index.tsx  --bundle --outdir=dist --format=esm --minify",
    "serve": "esbuild src/index.tsx  --bundle --outdir=dist --format=esm --servedir=dist --sourcemap"
  },
  "dependencies": {
    "esbuild": "^0.15.11",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
